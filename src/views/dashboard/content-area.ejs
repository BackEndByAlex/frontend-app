<div class="content-area">
  <%- include('../partials/flashmeddelande') %>

    <!-- Dashboard Section (Always visible by default) -->
    <section id="dashboard-section" class="dashboard-section active">
      <div class="page-header">
        <h1 class="page-title">Simplify Your Life!</h1>
        <p class="page-subtitle">Securely save and manage your passwords in one place.</p>
      </div>
    </section>

    <!-- Note Section -->
    <section id="note-section" class="dashboard-section">
      <div class="page-header">
        <h1 class="page-title">Simplify Your Life!</h1>
        <p class="page-subtitle">Securely save and manage your passwords in one place.</p>
      </div>
      <!-- Notes content here -->
    </section>

    <!-- Settings Section -->
    <section id="settings-section" class="dashboard-section">
      <div class="page-header">
        <h1 class="page-title">Simplify Your Life!</h1>
        <p class="page-subtitle">Securely save and manage your passwords in one place.</p>
      </div>
      <% if (isCodeVerified) { %>
        <%- include('./inställningar') %>
          <% } %>
    </section>

    <!-- Password Window (positioned over content-area) -->

    <% if (isCodeVerified) { %>
      <%- include('./password-modal') %>
    <% } %>
</div>